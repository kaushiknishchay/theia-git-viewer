{
  "private": true,
  "scripts": {
    "prepare": "lerna run prepare",
    "rebuild:browser": "theia rebuild:browser",
    "start:browser": "yarn rebuild:browser && yarn --cwd browser-app start"
  },
  "devDependencies": {
    "lerna": "2.4.0"
  },
  "workspaces": [
    "theia-git-viewer", "browser-app" ]
}
